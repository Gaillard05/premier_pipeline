name: GitHub Actions Demo

on: push

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest # Exécute le job sur un serveur Linux

    steps:
      # Intentionnellement pas d'étape "checkout" pour provoquer l'échec
      - name: Run Python script
        run: python job.py

      # Étapes additionnelles (facultatives)
      - run: echo "🔴 This step should not succeed because 'job.py' is missing."
